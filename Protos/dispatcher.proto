syntax = "proto3";

option csharp_namespace = "GrpcReverse";

service Dispatcher {
  rpc Register(stream Acknowledgement) returns (stream Message);
}

message Message {
  string id = 1;
  string content = 2;
}

message Acknowledgement {
  string id = 1;
  string status = 2;
}
